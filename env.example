# KPM Build Environment Configuration
# 复制此文件为 .env 并根据需要修改配置

# =============================================================================
# NDK 配置
# =============================================================================
# Android NDK 路径（可选，不设置则自动检测）
# NDK_PATH=/Users/yourname/Library/Android/sdk/ndk/23.1.7779620

# =============================================================================
# KernelPatch 配置
# =============================================================================
# KernelPatch 源码目录（可选，不设置则自动下载到 third_party/）
# KP_DIR=/path/to/KernelPatch

# KernelPatch 下载地址（可选，默认从 GitHub 下载）
# KP_ZIP_URL=https://github.com/bmax121/KernelPatch/archive/refs/heads/main.zip

# =============================================================================
# 构建选项
# =============================================================================
# 并行编译任务数（可选，默认自动检测 CPU 核心数）
# BUILD_JOBS=4

# CMake 构建类型（可选，默认 Release）
# BUILD_TYPE=Release

# 是否显示详细构建日志（可选，0=关闭，1=开启）
# VERBOSE_BUILD=0

